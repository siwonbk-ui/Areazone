# NAT CAT v2 — การใช้งานและเผยแพร่

## พฤติกรรมที่ตกลง

- โหมดพื้นฐาน: คงค่าที่เว็บใช้อยู่เดิม รวมค่า NAT CAT.xlsx ที่สกัดอยู่ใน `pipeline/districts.json` เมื่อข้อมูลใหม่ไม่พร้อม ไม่ตีความ null เป็นความเสี่ยงต่ำ
- โหมดสถานการณ์ล่าสุด: รายงานจากแหล่งทางการย้อนหลัง 7 วัน แยกจากสีพื้นฐาน มีเวลาตรวจสำเร็จ/เผยแพร่ และสถานะข้อมูลล่าช้า ไม่มีการย้อมสีจังหวัดจากข่าว
- ไม่มีไฟล์ NAT CAT.xlsx ต้นฉบับใน repo จึงใช้ค่าที่สกัดไว้เดิมโดยไม่อ้างว่าตรวจไฟล์ต้นฉบับใหม่แล้ว
- การตรวจอัตโนมัติยืนยันได้เฉพาะรูปแบบ แหล่งเผยแพร่ วันเวลา และตัวตนรายการพื้นที่ ไม่ยืนยันความเสียหายจริงหรือความถูกต้องเชิงวิชาการของเกณฑ์สี
- หากพบวันที่เหตุการณ์ไม่สมบูรณ์ ระบบกักชั้นภัยนั้นทั้งชั้นไว้ (`blockedHazards`) และคงสี/คำอธิบายเดิม แม้มีชั้นภัยอื่นรอตรวจและอนุมัติได้ตามปกติ

## ตารางการทำงาน

| ระบบ | รอบ | งาน |
|---|---|---|
| n8n | ทุกวัน 07:00 Asia/Bangkok | อ่านผลการตรวจจาก GitHub, สรุปความล่าช้า/แหล่งล่ม/รายการรอตรวจ |
| GitHub | นาทีที่ 17 ทุกชั่วโมง | เรียกตัวรวบรวมรายงาน, บันทึกผล, เผยแพร่หน้าเว็บ |
| DMR | n8n ดึงวันละครั้ง 11:00 น. | RSS รายงานธรณีพิบัติภัย ส่งต่อผ่าน n8n ในประเทศ (ดูหัวข้อ "DMR ผ่าน n8n") ไม่ได้อ่าน PDF เพื่อยืนยันขอบเขตภัย |
| TMD แผ่นดินไหว | ทุก 2 ชั่วโมง | RSS ประเทศไทยและประเทศใกล้เคียง; เคารพ ttl=120 ของ feed |
| ชุดข้อมูลประวัติ CKAN | ครั้งแรกของแต่ละวันตามเวลาไทย | สร้าง candidate หากแหล่งเปลี่ยน; หากผิดพลาด คงค่าที่เผยแพร่ไว้ |

GitHub schedules อาจล่าช้า ไม่ใช่การรับประกันรอบเรียลไทม์; หากต้องตรวจซ้ำหลัง CKAN ล่ม ใช้ Run workflow → force=true

## DMR ผ่าน n8n (relay)

dmr.go.th ตอบ HTTP 403 กับ GitHub Actions ทุกรอบ แต่ตอบ 200 กับเครื่องในประเทศไทยด้วย User-Agent เดียวกัน
(ทดสอบ 25 ก.ย. 2569) จึงเป็นการปฏิเสธ IP ต่างประเทศ/ดาต้าเซ็นเตอร์ ไม่ใช่ปัญหาโค้ด

1. n8n flow `n8n/natcat-dmr-relay.json` ดึงฟีดจากในประเทศ**วันละครั้ง 11:00 น.** (หลังเวลาที่ DMR มักออกรายงาน 09:00–10:00)
   และ commit `pipeline/feeds/dmr.json` **เฉพาะเมื่อเนื้อหาฟีดเปลี่ยน** รายงานที่ออกหลัง 11:00 จะเข้าระบบวันถัดไป
2. การ push ไฟล์นี้ทริกเกอร์ **Update NAT CAT data** ทันที `current.py` อ่านไฟล์แทนการยิงเว็บ และยังตรวจโดเมน
   รูปแบบ RSS วันเวลา และแหล่งที่มาเหมือนเดิมทุกข้อ
3. หน้าเว็บแสดง "ช่องทาง: ส่งต่อผ่าน n8n" และเวลา "ดึงสำเร็จ" คือเวลาที่ได้รับรายงานฉบับใหม่ล่าสุด
4. ถ้าไม่มีฉบับใหม่เกิน 60 ชม. สถานะเป็น `stale` → `update_status.json` เป็น `degraded` → n8n สรุปรายวันแจ้งเตือน
5. ก่อนมีไฟล์ relay ระบบยังดึงตรงรายชั่วโมงเหมือนเดิม (ซึ่งยังได้ 403)

ต้องใช้ fine-grained PAT เฉพาะ repo นี้ สิทธิ์ **Contents: Read and write** ใส่ใน n8n credential ชนิด Header Auth
(`Authorization: Bearer <token>`) ห้ามใส่ token ใน JSON

## การลด commit และการ deploy

- `pipeline/commit_gate.py` commit เฉพาะเมื่อเนื้อหาเปลี่ยน (รายงาน สถานะแหล่ง ผลตรวจ ข้อมูล) ไม่ commit ถ้าเปลี่ยนแค่เวลา
  (`checkedAt`, `lastAttemptAt`, `lastSuccessAt`, `baselineCheckedAt`, `baselineCheckDay`, `createdAt`, `builtAt`)
- commit heartbeat เมื่อเวลาตรวจที่ commit ไว้เก่ากว่า 2.5 ชม. เพื่อให้เกณฑ์ "การตรวจข้อมูลล่าช้า" 4 ชม. ของหน้าเว็บและ n8n ยังถูกต้อง
  (จากเดิม 24 commit/วัน เหลือราว 8 heartbeat/วัน บวกการเปลี่ยนแปลงจริง)
- `pipeline/state_cache.py` เก็บ `lastAttemptAt` และ `baselineCheckDay` ใน Actions cache ระหว่างรอบ เพื่อให้ยังเคารพรอบดึง
  TMD 2 ชม. และตรวจ baseline วันละครั้ง แม้ไม่ได้ commit; เวลาคืนค่าจะเลือกฉบับที่ใหม่กว่าระหว่าง cache กับ repo
- **Publish NAT CAT website** ถูกสั่งเฉพาะรอบที่ commit ข้อมูลจริง (เดิม deploy ทุกรอบผ่าน workflow_run)
- ไม่ได้แก้ `pipeline.py` / `review.py` จึงไม่ทำให้ code hash เปลี่ยนและไม่รีเซ็ต reviewHash ที่รออนุมัติ

## ขั้นตอนขึ้น GitHub

1. นำไฟล์เวอร์ชันนี้ขึ้น repo `siwonbk-ui/Areazone` branch `main` (รวม `.github/workflows`)
2. Settings → Pages → Source เลือก **GitHub Actions** เพื่อใช้ `pages.yml`
3. ตรวจว่า repository อนุญาต workflow เขียน contents และ deploy Pages
4. Run workflow **Update NAT CAT data** แล้วอ่าน `pipeline/update_status.json` และ Actions summary
5. ตรวจ workflow **Publish NAT CAT website** และเปิดเว็บทดสอบทั้งสองโหมด

ใช้ workflow_run เพื่อให้การ commit ข้อมูลด้วย GITHUB_TOKEN ตามด้วยการ deploy Pages ได้ ไม่อาศัย push trigger จาก bot เพียงอย่างเดียว
Pages artifact มีเฉพาะไฟล์หน้าเว็บ ไม่รวม pipeline, cache, source state และรายการรอตรวจ

## n8n

1. Import `n8n/natcat-daily-update.json` หรือไฟล์ `NAT CAT v2 — ตรวจผลรายวัน.json` ที่ให้แยกไว้
2. ปิด workflow เวอร์ชันเดิมเพื่อไม่ให้สั่งงานซ้ำ
3. Execute workflow ทดสอบหลังไฟล์ `pipeline/update_status.json` อยู่บน GitHub แล้ว
4. ดู output โหนดสรุป: `text` และ `needsAttention`
5. แทนโหนดปลายทาง noOp ด้วยช่องทางแจ้งเตือนที่ต้องการ แล้วเปิดใช้งาน

เวอร์ชันนี้ไม่ต้องใช้ PAT หรือ Gemini เพื่อสรุปผล public repo และไม่ dispatch GitHub ซ้ำ หาก repo เปลี่ยนเป็น private ต้องเพิ่ม credential สำหรับการอ่านเอง ไม่ใส่ token ใน JSON
โหนดแจ้งทีมยังไม่ส่งข้อความจริงจนกว่าจะตั้งค่าปลายทาง

## ตรวจและยืนยันการเปลี่ยนสีพื้นฐาน

`pipeline/pipeline.py` ไม่เขียนทับข้อมูลที่เผยแพร่แล้วโดยตรง:

1. สร้าง `pending_update.json`, `sources_candidate.json` และ `review_report.json`
2. ตรวจรายการพื้นที่/ภัยที่เปลี่ยน (`changes`), ปีข้อมูลใน `buildInfo`, หลักฐานรายพื้นที่ใน `ev`, ไฟล์ต้นทางใน `sourceState` และเกณฑ์สี โดยผู้รับผิดชอบข้อมูล/ความเสี่ยง
3. ถ้าข้อมูลยังไม่ชัดเจน ไม่ต้องอนุมัติ หน้าเว็บใช้ค่าเดิมต่อได้
4. หากยืนยันถูกต้อง ให้ Run workflow → ใส่ `approve_hash` เท่ากับ `reviewHash` เต็มจากรายงานที่ตรวจ ไม่ต้องเลือก force
5. ระบบตรวจ hash ของทั้งข้อมูลและ source state รวมถึงฐานเดิมก่อนเผยแพร่ บันทึกผู้ตรวจจาก github.actor และเวลา

การใส่ hash เป็นการยืนยันของผู้รับผิดชอบ ไม่ใช่การรับรองอัตโนมัติของ AI และยังไม่ใช่ระบบอนุมัติสองบุคคล
ข้อมูลใหม่ที่เป็น null หรือได้จาก fallback จะไม่ล้าง/ลดค่าที่มีอยู่เดิม หากไม่มีค่าเดิมใช้ xlsx_<hazard> เมื่อมี หากไม่มีทั้งคู่ยังเป็นไม่มีข้อมูล
การเปลี่ยนข้อมูลต้นทางระหว่างรอตรวจจะทำให้ candidate/hash เปลี่ยน ต้องตรวจฉบับล่าสุด

## แหล่งที่ใช้งานจริงและข้อจำกัด

- DMR: https://www.dmr.go.th/category/geohazard_daily_report/feed/
- TMD: https://earthquake.tmd.go.th/feed/rss_inside.xml
- ทั้งคู่ทดสอบนำเข้าได้ใน local วันที่ 23 ก.ย. 2569
- GISTDA น้ำท่วม/ภัยแล้ง, TMD พายุ/ลูกเห็บ, ThaiWater และ ปภ.รายงานเหตุ อยู่ในรายการลิงก์ประกอบ ยังไม่ได้เชื่อมอัตโนมัติ ไม่มีการอ้างว่าครบทุกภัยแล้ว
- ระบบยังไม่ตรวจ polygon ของภัยทับพิกัดโรงงาน ไม่มีข้อมูลสถานประกอบการใน repo
- ไม่มีรายงานใหม่ไม่ใช่หลักฐานว่าปลอดภัย รายงานที่ไม่ทราบพื้นที่จะไม่ถูกผูกเข้ากับจังหวัดจากการเดา
- ภาค/ค้นหาในโหมดล่าสุดกรองตามจังหวัดที่กล่าวถึงในชื่อรายงานเท่านั้น ไม่ใช่พื้นที่ผลกระทบ; รายงานไม่ทราบพื้นที่ยังแสดงไว้พร้อมป้าย

## ทดสอบ

```sh
python -m unittest discover -s tests -v   # รวม tests/test_relay_and_gate.py
python pipeline/current.py --force
python pipeline/run_updates.py
```

คำสั่งสุดท้ายติดต่อ CKAN และ feed จริง; เมื่อแหล่งล่มออกด้วยรหัส 1 พร้อมบันทึกสถานะ degraded โดยเก็บค่าพื้นฐานเดิม
`prepare_release.py` ใช้ครั้งเดียวเพื่อปรับ metadata และสร้างไฟล์นำเข้า n8n; ไม่เปลี่ยนสีพื้นฐาน

## ผลตรวจรอบเตรียมส่ง

- แก้การจับคอลัมน์ Province Code/District Code ที่เดิมอาจถูกเลือกแทน Province/District ทำให้ข้อมูลปี 2566–2567 บางชุดไม่ถูกนำมาคำนวณจริง
- น้ำท่วมปี 2562 เป็นจำนวนอำเภอในตารางสรุปจังหวัด จึงไม่ใช้เป็นเหตุการณ์รายอำเภอ
- น้ำท่วมปี 2563 มีวันที่อ่านไม่ได้/ว่าง 5,932 แถว จึงกักชั้นน้ำท่วมและคงค่าเดิม รอผู้ดูแลตรวจไฟล์ต้นทาง ห้ามเดาวันที่ให้ข้อมูล
- candidate หลังแก้การอ่านคอลัมน์มีการเปลี่ยนสี 728 คู่รายการพื้นที่–ภัย (วาตภัย 197, ลูกเห็บตัวแทน 448, ดินถล่ม 83) ยังไม่ได้อนุมัติ; ไม่ใช่ 728 อำเภอไม่ซ้ำ
- สีที่เผยแพร่เดิมทั้ง 1,212 รายการยังเหมือน HEAD เดิมทุกภัย
- ตรวจได้ว่า GitHub ยังไม่มี credential สำหรับ push บนเครื่องนี้ และยังไม่ได้ import/activate n8n หรือเปลี่ยน Pages settings จริง
